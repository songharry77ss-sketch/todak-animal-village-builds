name: Verify Mac runner

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: macos-26
    timeout-minutes: 5
    steps:
      - name: Verify Xcode availability
        run: xcodebuild -version
