# Todak Animal Village Release Builds

Public build automation for the Todak Animal Village app.

Application source, signing keys, provisioning profiles, tester data and private
development history are not stored in this repository. Release inputs use
short-lived download links stored as encrypted GitHub Actions secrets.

Workflows are manually dispatched by repository maintainers only.
